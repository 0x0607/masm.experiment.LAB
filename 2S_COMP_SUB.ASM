.MODEL	SMALL
.DATA
NUM1	DB 8
NUM2	DB 3
RESULT	DB ?
.CODE
MAIN:
	MOV AX,@DATA	
	MOV DS,AX
	MOV AL,NUM2	; AL = 3
	NOT AL		; 1's
	INC AL		; 2's
	MOV DL,NUM1	; DL = 8
	ADD DL,AL	; DL = 8+(-3)
	MOV RESULT,DL	; RESULT = 8+(-3)
	MOV AH,4CH
	INT 21H
.STACK
	END MAIN
